// Configuration defaults do not determine physical progression or success.
export const initialConfiguration={id:'transformation',startWeight:216,goalLow:192,goalHigh:204,laterMilestone:175,durationWeeks:12,weeklyLossLow:1,weeklyLossHigh:2};
const exercise=(id,name,muscles,equipment,type,regressions,alternatives,progressions,skill,cues)=>({id,name,targetMuscles:muscles,equipment,movementType:type,regressions,alternatives,progressions,dominanceRules:{skill,protocol:'activity-specific control and repeatability',requiresRepeatability:true},safety:{painStopsProof:true,explosive:false,highVolumeEligible:false,cues}});
export const initialExercises=[
 exercise('assisted-pullup','Assisted pull-up',['lats','upper back','biceps','grip'],['pull-up bar','band or assistance machine'],'vertical pull',['feet-assisted pull-up','scapular pull'],['lat pulldown'],['negative pull-up','strict pull-up'],'pull',['Use enough assistance for clean reps','Avoid swinging']),
 exercise('row','Dumbbell row',['lats','upper back','biceps'],['dumbbell','optional bench'],'horizontal pull',['lighter row'],['band row','machine row'],['heavier controlled row'],null,['Keep trunk stable','Avoid yanking the load']),
 exercise('curl','Biceps curl',['biceps','forearms'],['dumbbells'],'elbow flexion',['lighter curl'],['band curl','cable curl'],['heavier controlled curl'],'curl',['Keep elbows steady','Keep wrists neutral']),
 exercise('hang','Active hang',['grip','lats','upper back'],['pull-up bar'],'isometric hang',['feet-supported hang'],['assisted machine hang'],['longer controlled hang'],'pull',['Use support when needed','Retain shoulder control']),
 exercise('mobility','Controlled squat',['hips','quads','ankles'],['optional support'],'squat mobility',['supported shallow squat'],['supported sit-to-stand'],['deeper comfortable controlled range'],'mobility',['Never force painful depth']),
 exercise('carry','Farmer carry',['grip','core','upper back','legs'],['dumbbells'],'loaded carry',['lighter carry'],['suitcase carry'],['longer carry','heavier carry'],'carry',['Walk steadily','Maintain upright posture'])
];
export const initialGoals=[{id:'first-pullup',name:'First Strict Pull-Up',dependsOn:['hangs','scapular-control','assisted-pullups','negatives','grip-milestone'],physical:true},{id:'hangs',name:'Repeatable controlled hangs',dependsOn:[],skill:'pull',physical:true},{id:'scapular-control',name:'Scapular control',dependsOn:[],skill:'pull',physical:true},{id:'assisted-pullups',name:'Assisted pull-up command',dependsOn:['hangs','scapular-control'],physical:true},{id:'negatives',name:'Controlled negatives',dependsOn:['assisted-pullups'],physical:true},{id:'grip-milestone',name:'Grip milestone',dependsOn:[],physical:true}];
export function validateGoalGraph(goals){const graph=new Map(goals.map(g=>[g.id,g]));const visiting=new Set(),done=new Set();function visit(id){if(visiting.has(id))throw Error('Goal dependencies contain a cycle.');if(!graph.has(id))throw Error('Goal dependency is missing: '+id);if(done.has(id))return;visiting.add(id);for(const d of graph.get(id).dependsOn)visit(d);visiting.delete(id);done.add(id)}for(const id of graph.keys())visit(id);return true}
export function evaluateGoal(id,goals,evidence){validateGoalGraph(goals);const graph=new Map(goals.map(g=>[g.id,g]));function evaluate(id){const goal=graph.get(id);if(!goal)throw Error('Goal not found.');return {id,proven:evidence.some(e=>e.goal===id&&e.proven===true)&&goal.dependsOn.every(d=>evaluate(d).proven),dependencies:goal.dependsOn.map(evaluate)}}return evaluate(id)}
